/* global describe, expect, test */

// import { selectWalkDomain } from '../selectors';

describe('selectWalkDomain', () => {
  test('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
