/* global describe, expect, test */
// import React from 'react';
// import { shallow } from 'enzyme';

// import { GalleryListItem } from '../index';

describe('<GalleryListItem />', () => {
  test('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
